'use strict';var extend = require('extend-shallow');function toRegex(pattern,options){return new RegExp(toRegex.create(pattern,options))}toRegex.create = function(pattern,options){if(typeof pattern!== 'string'){throw new TypeError('expected a string')},options);if(opts && opts.contains === true){opts.strictNegate = false}var open = opts.strictOpen!== false ? '^' :'';var close = opts.strictClose!== false ? '$' :'';var endChar = opts.endChar ? opts.endChar :'+';var str = pattern;if(opts && opts.strictNegate === false){str = '(?:(?!(?:'+pattern+')).)'+endChar}else{str = '(?:(?!^(?:'+pattern+')$).)'+endChar}return open+str+close};module.exports = toRegex;