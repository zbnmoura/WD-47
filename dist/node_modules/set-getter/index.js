/*!
 * set-getter (https://github.com/doowb/set-getter)
 *
 * Copyright (c) 2016, Brian Woodward.
 * Licensed under the MIT License.
 */'use strict';var toPath = require('to-object-path');function setGetter(obj,prop,getter){var key = toPath(arguments);return define(obj,key,getter)}function define(obj,prop,getter){if(!~prop.indexOf('.')){defineProperty(obj,prop,getter);return obj}var keys = prop.split('.');var last = keys.pop();var target = obj;var key;while((key = keys.shift())){while(key.slice(-1) === '\\'){key = key.slice(0,-1)+'.'+keys.shift()})}defineProperty(target,last,getter);return obj}function defineProperty(obj,prop,getter){Object.defineProperty(obj,prop,{configurable:true,enumerable:true,get:getter})}module.exports = setGetter;