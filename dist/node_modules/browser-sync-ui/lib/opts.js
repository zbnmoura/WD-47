var Immutable = require("immutable");var defaults = Immutable.fromJS({port:3001});module.exports.merge = function(obj){return defaults.mergeDeep(Immutable.fromJS(obj))};//function transformOptions(obj){// // var out;// // Object.keys(transforms).forEach(function(key){// out = obj.set(key,transforms[key](obj));//});// // return out;//}