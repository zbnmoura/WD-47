var clone = require('clone');module.exports = function(options,defaults){;Object.keys(defaults).forEach(function(key){if(typeof options[key] === 'undefined'){options[key] = clone(defaults[key])}});return options};